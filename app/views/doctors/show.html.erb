<h3><%=@doctor.name%></h3>
<ul>
<%@doctor.appointments.each do |app|%>
    <li><%= app.appointment_datetime.strftime("%B %d, %Y at %R")%></li>
    <li><%= link_to app.patient.name, patient_path(app.patient)%></li>
<%end%>
</ul>
